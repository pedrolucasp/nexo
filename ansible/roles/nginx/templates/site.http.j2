# Pre-certificate server block: only answers ACME challenges
server {
    listen 80;
    server_name {{ vhosts[0] }};

    location ^~ /.well-known {
        root /var/www;
    }

    location / {
        return 204;
    }
}
